local({r <- getOption("repos")
  r["CRAN"] <- "https://cloud.r-project.org/"
  options(repos=r)})

options(max.print=100)
options(show.signif.stars=FALSE)

q <- function (save="no", ...) {
  quit(save=save, ...)
}

message("*** Successfully loaded project's .Rprofile ***")
